// Phase 1 placeholder — proves the Vite+React build/deploy pipeline works
// end-to-end before any real wizard logic is ported. See the migration plan.
function App() {
  return (
    <div style={{ fontFamily: 'sans-serif', padding: '40px', maxWidth: '600px', margin: '0 auto' }}>
      <h1>Registration wizard — React migration (Phase 1)</h1>
      <p>
        This is a placeholder. If you can see this page, the Vite build and
        the Azure Static Web Apps deploy pipeline are both working correctly.
        No wizard functionality has been ported yet.
      </p>
    </div>
  )
}

export default App
